name: Defaults

run-name: Default configuration using GitHub Actions

on: [push]

defaults:
  run: 
    shell: bash
    working-directory: ./scripts

jobs:
  explore:
    runs-on: ubuntu-latest
    
    steps:
      - run: echo "This is the first command"
      - run: echo "This is the second command"
