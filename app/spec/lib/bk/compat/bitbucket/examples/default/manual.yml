pipelines:
  default:
    - step:
        name: simple
        trigger: 'automatic'
        script:
          - echo 'running'
    - step:
        name: complex
        script:
          - echo 'running'
        trigger: 'manual'
